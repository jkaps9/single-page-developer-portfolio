<div class="logo">
  <p>adamkeyes</p>
</div>

<style lang="scss">
  .logo {
    flex: 1;
    position: relative;
    z-index: 10;

    p {
      color: var(--color-header-text);
      font-size: clamp(1.5rem, 1.3239rem + 0.7512vw, 2rem);
    }
  }
</style>
